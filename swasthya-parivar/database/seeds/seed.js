const mongoose = require('mongoose');
const User = require('../../backend/src/models/User');

const seedUsers = async () => {
  console.log("Seeding database with default admin & worker...");
};
seedUsers();